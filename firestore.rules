rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permite leitura e escrita em qualquer documento e subcoleção se o usuário estiver autenticado
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}