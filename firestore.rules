rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissão global para qualquer usuário autenticado
    // Isso resolve o erro de permissão para leitura e escrita em qualquer coleção
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}