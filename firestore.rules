rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Permissão total para qualquer usuário autenticado no sistema de auditoria
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}